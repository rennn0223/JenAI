# JenAI v0.33.0 - 驗證策略定調:Isaac Sim 為主 + 部署事實更正

**兩個事實更正(操作機是 DGX Spark、本地預設模型 qwen3.6:35b)加一個方向定調:v1.0 的驗收證據全部在 Isaac Sim 上取得,實體驗證降為選配/交接下一屆。**

## 驗證策略定調(V1_GATE 層二重定義)

- **v1.0 只在 Isaac Sim 上驗收**:接口確認(B1)、地點建置(B2)、TEST.md 實測(B3)、里程與任務數(B4,模擬 ≥20h/≥50 次)、Twin 消融(B5)、onboarding 計時(B6)、demo(B7)全部改在孿生場景完成;B5 場景建置成為關鍵路徑第一步
- **實體驗證移入新的選配表 P1–P3**(實車接口清點、實車里程、真機冒煙),不擋 v1.0,有機會再做或交接下一屆——架構載具無關、twin 與實車走同一條 bridge/Nav2 介面,實體驗證不改程式、只補數據
- sim-to-real 落差於論文誠實標註為 limitation / future work;PROJECT_DIRECTION 新增「驗證策略更新」章,ROADMAP 現況快照、軌道 3、里程碑表、兩層對照同步改寫

## 部署事實更正(全庫對齊)

- **操作機是 DGX Spark,不是 Jetson**:TECHNICAL_GUIDE、TEST、THREAT_MODEL、TWIN_SETUP、PROJECT_DIRECTION、providers README 全數更正
- **本地預設模型 `qwen3.6:35b`**:setup wizard 預設值與提示、`/model` 用法示例(TUI 與文件)、QUICKSTART 的 `ollama pull`、TECHNICAL_GUIDE 設定範例(vision 一併 pin 明確 tag,不用會漂的 `latest`)
- CLAUDE.md 角色分工同步:客戶端驗測以 Isaac Sim 為主(DGX Spark 工作站),實體操作屬選配

## 驗證

- Ruff 全綠;Python 3.12 / 3.13 / 3.14 完整測試全綠
- PR CI `test` 與 `build` jobs 全綠

**Full Changelog**: https://github.com/rennn0223/JenAI/compare/v0.32.1...v0.33.0
