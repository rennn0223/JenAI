# JenAI v0.30.1 - 文件大掃除 + 小白手冊 + Wizard 換裝

**應使用者要求的整理版:文件全面對齊 v0.30 實況、新增零基礎上手手冊、Setup Wizard 穿上 TUI 同款橘。**

## Setup Wizard 換 TUI 橘(視覺統一)
- banner 從 cyan→magenta 漸層改為**淺→深暖橘**(`#f2b28c`→`#c15f3c`);三步面板、選項編號改品牌橘 `#d97757`;完成框改 TUI 綠 `#7d9b6a`——首次體驗與主 TUI 終於同一套色系
- 順手清掉 `tui/panels.py` 的誤名常數 `BLUE`(值其實是橘 `#d97757`),command marker 直接用 `ACCENT`

## 新增 docs/QUICKSTART.md(零基礎上手手冊)
- 不熟終端機也能照做:裝 uv → clone → 設定精靈(三步逐步)→ doctor 白話解讀 → 第一句對話 → 第一次 `/route`(批准卡 1/2/3)
- **常見狀況對照表**:unavailable=誠實回報不是壞掉、401=token 換了、`onboard` 重設不丟金鑰、shell export 蓋過 `.env`、launcher symlink…
- 掛進 docs 索引與根 README 最頂端——補上「小白無入口」的缺口

## 文件對齊 v0.30(逐項對碼稽核)
| 文件 | 修了什麼 |
|---|---|
| TECHNICAL_GUIDE | 版本頭 v0.28→v0.30;模組表行數全面校正(app 1715、ros_bridge 922、server 717…)並註明「量級參考」;**補 NavigationGateway/twin gate/state(SQLite audit)/setup wizard 四列**;cli 補 `onboard`;對話列改為模式路由敘述 |
| CODE_TOUR | 版本頭 v0.30;cli 627 行 + onboard;ros_bridge 922 |
| V1_GATE | 快照 v0.23.0 → v0.30.0 |
| COMMANDS | Session 表補 `/mode` 正式列 |
| HANDOFF | 能力清單補 v0.24–v0.30(避障重寫/Gateway/queue/批准恢復/audit/onboard) |
| PROJECT_DIRECTION | 架構圖補實作註記(NavigationGateway 單一出口 + audit) |
| TEST | 本機實測快照誠實標註「v0.22.1 實測,重跑後更新」 |
| help_content | ROS2 群組補漏列的 `/ros drive`(與 COMMANDS 同源) |

**稽核同時驗證通過(無需修)**:WebUI 面板與 `_slash` 同源、confirm token 120s 文件與程式一致、`/queue` 全處收錄、17 個目錄 README 無缺。

## 驗證
- 430+ 測試全綠;ruff 全綠;wizard 新配色以 rich 實際渲染核對(截圖)
- 交接文件現況:HANDOFF(狀態/方法/誠實指正/終章)+ QUICKSTART(新手)+ TECHNICAL_GUIDE/CODE_TOUR(工程師)+ V1_GATE/ROADMAP(驗收與前瞻)——四種讀者各有入口

**Full Changelog**: https://github.com/rennn0223/JenAI/compare/v0.30.0...v0.30.1
