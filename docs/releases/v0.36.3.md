# JenAI v0.36.3 - 冷啟動 bridge 的 Nav2 發現競態修正

**E2 消融實驗量出來的真 bug:每次預演起全新 twin bridge,`wait_for_server(2.0)` 在 111-topic 忙碌 graph 上約 45% 冷啟動輸給 DDS 發現,產生可避免的「Nav2 not running」誤 refer(正常目標誤攔率被灌到 55%)。**

## 修正

- bridge `nav_send` 的 action server 發現等待 2s → **10s**:Nav2 在場時發現完成即刻返回(不多等);真不在時多等 8 秒才誠實 refer——語意不變,消除競態
- 影響面:Twin Gate 每次預演的新 bridge、載具側冷啟後首次導航

## 出處

E2 v2(100 趟,seed 20260715)數據異常自查:9/20 正常目標以同一錯誤在 ~2.6s 失敗、時間特徵與 2.0s 等待完全吻合;go_home 所用之長壽命 bridge 同期零失敗(發現已暖),交叉定位為冷啟動競態。實驗數據以 v3 重跑。

**Full Changelog**: https://github.com/rennn0223/JenAI/compare/v0.36.2...v0.36.3
