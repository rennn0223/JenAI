# JenAI v0.34.1 - Run blocked 不再吞掉原因 + B3 全鏈實測回填

**agent 代測 B3(Isaac Carter 場景)發現的 UI 誠實性 bug:orchestrator 把 run 被擋的完整原因寫進 final_output,TUI 卻只印「Run blocked.」四個字。**

## 修正

- **TUI blocked 分支顯示 final_output**:模型迴圈被攔時,操作者現在看得到完整解釋與建議指令(例如「改用 /ros drive 直接執行」),不再只有四個字——誠實回報不該止步於 orchestrator

## B3 實測回填(TEST.md,Isaac Carter 場景,agent 以 tmux 驅動真 TUI)

- `/route` ✅:兩段導航判讀正確、剩餘距離即時遞減、**Esc 取消後 5 秒位移 0.0 cm**(物理實measured)、失敗段誠實 n/m
- `/mission` ✅ 2/2;`/patrol x1 photo` ✅ 2/2 + 每點 👁 VLM 場景描述;`/report` ✅ 確定性日報 + zh-TW LLM 摘要
- `/perception` ✅:affordances(#path_clear #forklift_present)、建議動作標「需批准」、誠實幀數
- 現況快照更新為 Carter 場景(doctor 全 pass、導航層全可測、twin 剩孿生側)

## Known issues(記錄於 TEST.md 快照節,未修)

1. route 解析器對 destination-only 句式(`去X`、`Go to X`)失敗——`/run` agent 內部第一次嘗試也踩到
2. `/run` 以 qwen3.6:35b:工具鏈全通,但模型 resume 後重複同一動作 → 迴圈偵測誠實 BLOCKED(論文小模型失效模式素材)
3. 貼牆存的點(map_left_up)終點逼近會 Nav2 abort——點位問題,`/loc move` 重存即修

## 驗證

- 全測試綠、ruff 綠、PR CI `test` + `build` 全綠

**Full Changelog**: https://github.com/rennn0223/JenAI/compare/v0.34.0...v0.34.1
