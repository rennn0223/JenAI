# JenAI v0.36.0 - Twin Gate 端到端上線(倒置佈局)

**使用者的好主意「Twin 跟實際倒過來」實測成立:現有已驗證場景直接當孿生(domain 0),載具側搬去 42——不等第二個 Isaac 實例,Twin-Gated Execution 今天就完成了第一次真實過閘。**

## Twin Gate 端到端實錄(2026-07-15,Isaac Carter)

- **pass 路**:`/route 去map_wall` → 孿生(domain 0 的 Carter)實際預演 56 秒、G1–G5 全評、verdict=pass 進 audit → 載具側(42,空)誠實回報「goal was NOT sent」
- **block 路**:`/route 去sw_test_zone`(目標在 `SW-narrow-aisle` 禁區)→ **G3 block,「the real robot was NOT moved」**,verdict 與判準明細進 audit
- G1 contact sensor 未建時誠實 skip(doctor warn 指路 TWIN_SETUP §3)

## 新增:doctor `twin_isolation` 守門

倒置佈局讓「孿生與載具共用 domain」的誤設風險升高——共用時**預演會直接開動真車**。doctor 現在比對 `[twin] domain_id` 與環境 `ROS_DOMAIN_ID`,相同即 **fail** 並拒絕往下探測(含 twin=0 且環境未設定的情境);附單元測試。

## 文件

- TWIN_SETUP §1 新增**倒置佈局**(sim-first 推薦):現有場景=孿生、`[twin] domain_id=0`、JenAI 以 `ROS_DOMAIN_ID=42` 啟動;傳統佈局照舊支援
- V1_GATE B5 → 🚧(pipeline 兩路實測過閘,剩 G1 感測器與消融數據);TEST.md 端到端預演列 → ✅

## 驗證

- 全測試綠、ruff 綠、PR CI 全綠;gate verdict/audit 實錄見上

**Full Changelog**: https://github.com/rennn0223/JenAI/compare/v0.35.0...v0.36.0
