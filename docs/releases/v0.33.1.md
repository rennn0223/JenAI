# JenAI v0.33.1 - Twin 建置文件重寫:四階段照做就通

**應使用者回饋(TWIN_SETUP 不完善不好懂)的文件修訂:把「建場景 → 跑 Nav2 → 接 JenAI → 開 Gate」寫成有順序、有檢查點的四階段走法,並對齊 sim-first 驗證策略。**

## TWIN_SETUP.md 重寫

- 新增 **§0 建置總覽**:四階段表(官方樣例 → 自建場景 → 接 JenAI → 孿生側+Gate),每階段標註照哪份文件、完成檢查點;明示「先讓載具側能跑,再加孿生側」,Gate 不是場景建置的一部分
- **sim-first 敘事對齊**:載具與孿生都是 Isaac 場景(兩實例、兩 domain);「與真車同一張地圖」改為「與載具側同一張佔位圖(即階段 2 產出)」;孿生側可 headless 或只在 Gate 實驗時開
- **接觸感測器(G1)從一句話擴成可照做的三步**:掛 Contact Sensor 到底盤 rigid body → Action Graph 三節點(On Playback Tick → Isaac Read Contact Sensor → ROS2 Publisher/Bool)→ `ros2 topic echo` 驗收
- 新增**載具側接上 JenAI** 節(route_adapter 切換、doctor nav、建點、`/route` 實測 = 解鎖 B3/B4)
- 禁區座標補查法(RViz Publish Point / `/clicked_point`);疑難排解補「兩實例搶 GPU 致 G2 超時」「感測器掛錯 prim」「平日跑 B3/B4 可關 Gate」

## 其他

- ISAAC_NAV2_SETUP.md 頂部標明它是四階段中的階段 1–2,與 TWIN_SETUP §0 互鏈

## 驗證

- 純文件修訂,無程式行為變更;PR CI `test` + `build` 全綠

**Full Changelog**: https://github.com/rennn0223/JenAI/compare/v0.33.0...v0.33.1
